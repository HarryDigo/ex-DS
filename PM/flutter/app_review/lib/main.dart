import 'package:flutter/material.dart'; //imports do material e do my app, sendo o arquivo inicial da execução
import 'src/my_app.dart';

void main() { //main que apenas liga o aplicativo
    runApp(const MyApp());
}