import 'package:flutter/material.dart';

var appBarGen = AppBar(
    title: const Text ("bolas numero 4", style:titleStyle),
    centerTitle: true,
);

const titleStyle = TextStyle(
    fontSize: 14,
    fontFamily: "Arial",
    color: Color(0xff0000ff)
);